fun nonEmpty (xs [List]) [returns {[Top] list_match choose[List] { case nil => nil  case cons v unused => cons (choose[List]) nil } }] = {
  list_match xs {
    case nil => cons nil nil
    case cons x ys => cons (cons nil nil) nil
  }
}

val x [{[List] cons (cons nil nil) nil}] = nonEmpty (cons 0 nil);

()
