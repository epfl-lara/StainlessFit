fun headOrElse(l [List])(default [Top])  [returns Top] = {
  list_match l {
    case nil => default
    case cons x xs => x
  }
}

fun example (l [List]) (default [Top])  [returns { [Top] 1 }] = {
  headOrElse (cons 1 l) default
}
